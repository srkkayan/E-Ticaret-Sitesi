@model IEnumerable<OrderProduct>
<div class="container">
    <div class="row">
        <div class="col-6">
            <h2>Ürün Listesi</h2>
        </div>
        <div class="col-6">
            </a>
        </div>
    </div>
    <div class="row">
        <table class="table table-bordered table-striped my-2">
            <thead>
                <tr>
                    <th>
                        Sipariş Id
                    </th>
                    <th>
                        Ad Soyad
                    </th>
                    <th>
                        Fiyat
                    </th>
                    <th>
                        İşlemler
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var orderProduct in Model){
                    <tr>
                        <td>@orderProduct.Id</td>
                        <td>@orderProduct.Name</td>
                        <td>@orderProduct.OrderPrice</td>
                        <td>
                            <a onclick="return confirm('Silmek İstediğinize Emin Misiniz');" asp-area="Customer" 
                            asp-controller="Order" asp-action="Delete" asp-route-id="@orderProduct.Id" class="btn btn-danger">
                                Sil
                            </a>
                        </td>
                    </tr> }
            </tbody>
        </table>
    </div>
</div>